Vagrant.configure("2") do |config|
  config.vm.box = "ubuntu/focal64"
  config.vm.network "public_network", ip: "seu-ip"
   config.vm.provider "virtualbox" do |vm|
     vm.name = "vagrant-ansible"
   end 
end
